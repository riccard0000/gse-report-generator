{
  "name": "gse-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy per GSE Report Generator",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
