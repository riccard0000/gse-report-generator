# FILE DISATTIVATO SU QUESTO BRANCH
# Il deploy.yml di main (GitHub Pages) viene sovrascritto da questo placeholder
# per evitare che venga eseguito su feature/azure-deploy.
# La pipeline Azure e' in deploy-azure.yml

name: Deploy to GitHub Pages (disabled on azure branch)

on:
  push:
    branches:
      - NEVER_RUN_THIS_BRANCH

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pipeline GitHub Pages disabilitata su questo branch."
